// Custom commands can be added here for reuse across specs.
// See https://docs.cypress.io/api/cypress-api/custom-commands

// Example:
// Cypress.Commands.add('login', (email, password) => {
//   cy.request('POST', '/login', { email, password });
// });
